
function getDisplVariant(product,variant){if(currentLang==='it'&&product.variants&&product.variantsIt){const vIndex=product.variants.indexOf(variant);if(vIndex>-1&&product.variantsIt[vIndex])
{return product.variantsIt[vIndex];}}
else
{return variant;}}
function getDisplSize(product,size){if(currentLang==='it'&&product.sizes&&product.sizesIt){const vIndex=product.sizes.indexOf(size);if(vIndex>-1&&product.sizesIt[vIndex])
{return product.sizesIt[vIndex];}}
else
{return size;}}
function getTranslatedList(product,selectedValues,type){if(!selectedValues||selectedValues.length===0)return[];let deSource=product[type];let itSource=product[type+'It'];if(typeof deSource==='string')deSource=deSource.split(',').map(s=>s.trim());if(typeof itSource==='string')itSource=itSource.split(',').map(s=>s.trim());if(currentLang!=='it'||!itSource)return selectedValues;return selectedValues.map(val=>{const idx=deSource?deSource.indexOf(val):-1;return(idx>-1&&itSource[idx])?itSource[idx]:val;});}
function getTranslatedExtras(selectedNames,product){if(!selectedNames||selectedNames.length===0||currentLang!=='it'){return selectedNames;}
const availableExtras=product.extras||[];return selectedNames.map(selectedName=>{const extraObj=availableExtras.find(e=>e.name.trim()===selectedName.trim());return(extraObj&&extraObj.nameIt)?extraObj.nameIt:selectedName;});}
function renderCart(){const container=document.getElementById('cart-container');const itemTemplate=document.getElementById('template-cart-item');if(!container||!itemTemplate)return;container.innerHTML="";let totalItems=0;orderCart.forEach((product,index)=>{const itemClone=itemTemplate.content.cloneNode(true);const isPizza=product.isPizza||product.category==='pizza';totalItems+=product.quantity;let displayVariant=getDisplVariant(product,product.orderDetails.variant);let displaySize=getDisplSize(product,product.orderDetails.size);itemClone.querySelector('.item-name').innerText=displayVariant,itemClone.querySelector('.qty-display').innerText=product.orderDetails.quantity;const descElement=itemClone.querySelector('.item-desc');const sel=product.orderDetails||{size:"",variant:"",extras:[],without:[]};if(isPizza){if(sel.size){descElement.innerHTML=`<span style="color: #ffa500; font-size: 1.2rem; font-weight: bold; display: block; margin-bottom: 5px;">${displaySize}</span>`;descElement.style.display='block';}
else{descElement.style.display='none';}}
else{itemClone.querySelector('.item-name').innerText=displayVariant;itemClone.querySelector('.item-desc').innerText=displaySize;}
const withoutDiv=itemClone.querySelector('.item-without');const currentWithout=sel.without||[];if(currentWithout.length>0){withoutDiv.style.color="#28a745";withoutDiv.style.display="block";withoutDiv.querySelector('.without-prefix').innerText=t.without+": ";const translatedNames=getTranslatedList(product,currentWithout,'desc');const prefix=currentLang==='it'?"Senza: ":"Ohne: ";const names=currentWithout.map(e=>e.name||e);withoutDiv.querySelector('.without-list').innerText=translatedNames.join(', ');}
else{withoutDiv.style.display='none';}
const extrasDiv=itemClone.querySelector('.item-extras');const currentExtras=sel.extras||[];if(currentExtras.length>0){extrasDiv.style.display="block";const translatedExtras=getTranslatedExtras(currentExtras,product);const prefix=currentLang==='it'?"Extra: ":"Extras: ";extrasDiv.querySelector('.extras-list').innerText=prefix+translatedExtras.join(', ');}
else{extrasDiv.style.display='none';}
let preis=calculateItemPrice(product);itemClone.querySelector('.item-price').innerText=preis.toFixed(2);itemClone.querySelector('.btn-minus').onclick=()=>changeCartQty(index,-1);itemClone.querySelector('.btn-plus').onclick=()=>changeCartQty(index,1);const btnEdit=itemClone.querySelector('.btn-edit');if(btnEdit){if(product.isStandard){btnEdit.style.display='none';}
else{let currentEditItem=null;btnEdit.onclick=()=>{editProduct=product;editIndex=index;;editWkId=product.wkId;plusMinusMenu(product,1);};if(btnEdit){btnEdit.innerText=translations[currentLang].edit;}}}
const itemToEdit=JSON.parse(JSON.stringify(product));itemToEdit.quantity=1;itemToEdit.isEditing=true;container.appendChild(itemClone);});document.getElementById("cart-count-badge").innerHTML=countTotal;}
function toggleCart(show){const cart=document.getElementById('cart-sidebar');cart.style.display=show?'flex':'none';}
function clearFullCart(){const confirmMsg=t.emptyConfirm||"Bestellung wirklich löschen?";if(confirm(confirmMsg)){orderCart=[];currentCart=[];renderCart();resetMenuQuantities();}}
function resetMenuQuantities(){const qtyLabels=document.querySelectorAll('.qty-display');qtyLabels.forEach(label=>{label.innerText="0";});}
function closeSuccessModal(){document.getElementById('order-success-modal').style.display='none';toggleCart(false);}
function addToOrderCart(product){orderCart.push(product);renderCart();renderMenu();}
function changeQuantityOrder(cartItemId,delta){let item=orderCart.find(i=>i.id===cartItemId);let found=orderCart.find(p=>p.id==item.id);if(item){item.quantity+=delta;countTotal+=delta;found.quantity+=delta;if(countTotal==-1){countTotal=0;}
if(found.quantity<0){found.quantity=0;}
if(item.quantity<=0){orderCart=orderCart.filter(i=>i.id!==cartItemId);}}
renderCart();renderMenu();}
function changeCartQty(index,delta){const item=orderCart[index];if(delta>0){item.orderDetails.quantity++;}else{item.orderDetails.quantity--;if(item.orderDetails.quantity<=0){orderCart.splice(index,1);}}
updateAllQuantities();renderCart();renderMenu();}
function calculateItemPrice(item){let basePrice=parseFloat(item.price.toString().replace(",","."));let extrasTotal=0;if(item.orderDetails&&item.orderDetails.extras){item.orderDetails.extras.forEach(extraName=>{const extraProduct=menuCart.find(p=>p.name===extraName&&p.category==='extra');if(extraProduct){extrasTotal+=parseFloat(extraProduct.price.toString().replace(",","."));}});}
return(basePrice+extrasTotal)*item.orderDetails.quantity;}
function sumExtras(prod){let prArr=prod.orderDetails.extrasPrice||[];return prArr.reduce((acc,priceString)=>{let cleanPrice=parseFloat(priceString.replace('+','').replace(',','.'))||0;return acc+cleanPrice;},0);}
function processCheckout(btn){if(tischNr!="Abholung"){processCheckout0(btn);}
else{openContactModal(btn);}}
async function processCheckout0(btn){if(!window.orderCart||orderCart.length===0)return null;try{if(btn)btn.disabled=true;const hasPizza=window.orderCart.some(item=>item.category==='pizza'||item.category==='kinderpizza'||item.isPizza===true);const hasDrinks=window.orderCart.some(item=>item.category!=='pizza'&&item.category!=='kinderpizza'&&item.category!=='extra');const artikelDetails=window.orderCart.map(item=>{let vollerName=item.name;let zusatz=item.orderDetails.size||"";if(item.orderDetails?.extras?.length>0){vollerName+=" + "+item.orderDetails.extras.join(", ");}
if(item.orderDetails?.removed?.length>0){vollerName+=" (OHNE: "+item.orderDetails.removed.join(", ")+")";}
return{artikel:vollerName+" "+zusatz,qty:item.orderDetails?.quantity||1,preis:(parseFloat(item.price.toString().replace(',','.'))+
(item.orderDetails?.extrasPrice||[]).reduce((s,p)=>s+parseFloat(p.toString().replace('+','').replace(',','.')),0)).toFixed(2),orderDetails:item.orderDetails};});const gesamtSumme=window.orderCart.reduce((sum,item)=>{const basePrice=parseFloat(item.price.toString().replace(',','.'))||0;const extrasTotal=(item.orderDetails?.extrasPrice||[]).reduce((eSum,ePrice)=>{const cleanPrice=parseFloat(ePrice.toString().replace('+','').replace(',','.'))||0;return eSum+cleanPrice;},0);const qty=item.orderDetails?.quantity||1;return sum+((basePrice+extrasTotal)*qty);},0);const orderData={tisch:document.getElementById('tisch-view')?.innerText||"Gast",details:artikelDetails,gesamtpreis:parseFloat(gesamtSumme.toFixed(2)),zeit:new Date().toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'}),status:'neu',needsPizzaReady:hasPizza,needsDrinksReady:hasDrinks,pizzaReady:false,drinksReady:false,location:(typeof coords!=='undefined'&&coords)?{lat:coords.latitude,lng:coords.longitude}:"Kein GPS",coords:(typeof coords!=='undefined')?coords:null,adresse:(typeof adresse!=='undefined')?adresse:null};const response=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(orderData)});if(response.ok){const result=await response.json();window.orderCart=[];const badge=document.getElementById('cart-count-badge');if(badge)badge.innerText="0";if(window.menuCart){window.menuCart.forEach(item=>{item.quantity=0;if(item.orderDetails){item.orderDetails.quantity=0;item.orderDetails.extras=[];item.orderDetails.removed=[];item.orderDetails.extrasPrice=[];}});}
if(typeof renderMenu==="function")renderMenu();if(typeof toggleCart==="function")toggleCart(false);return result.orderId;}else{alert("Fehler beim Senden an den Server.");if(btn)btn.disabled=false;return null;}}catch(err){console.error("Fehler im Checkout-Prozess:",err);if(btn)btn.disabled=false;return null;}}
function openContactModal(btn){if(typeof toggleCart==="function")toggleCart(false);document.getElementById('contactModal').style.display='flex';}
document.addEventListener('DOMContentLoaded',()=>{const btnSave=document.getElementById('btnSaveContact');const btnBack=document.getElementById('btnCancelContact');if(btnBack){btnBack.onclick=function(){document.getElementById('contactModal').style.display='none';if(btnSave){btnSave.disabled=false;btnSave.innerText="OK";}};}
if(btnSave){btnSave.onclick=function(){const vorname=document.getElementById('fname').value.trim();const nachname=document.getElementById('lname').value.trim();const telefon=document.getElementById('phone').value.trim();if(!vorname||!nachname||telefon.length<9){alert("Bitte alle Felder ausfüllen.");return;}
btnSave.disabled=true;if(isGeolocationActive===true){btnSave.innerText="Standort wird ermittelt...";if("geolocation"in navigator){navigator.geolocation.watchPosition(async(position)=>{const c={lat:position.coords.latitude,lng:position.coords.longitude};if(!orderGesendet){orderGesendet=true;await finishOrder(c,vorname,nachname,telefon);}
if(window.currentOrderId){sendGpsUpdate(window.currentOrderId,c.lat,c.lng);}},(err)=>{console.error("GPS Fehler:",err);if(!orderGesendet){alert("Standort benötigt! Bitte GPS erlauben.");btnSave.disabled=false;btnSave.innerText="Erneut versuchen";if(btnBack)btnBack.style.display="inline-block";}},{enableHighAccuracy:true,timeout:10000,maximumAge:0});}
else{alert("GPS nicht unterstützt.");btnSave.disabled=false;}}
else{btnSave.innerText="Bestellung wird gesendet...";const dummyCoords={lat:0,lng:0};if(!orderGesendet){orderGesendet=true;finishOrder(dummyCoords,vorname,nachname,telefon);}}}}});async function finishOrder(coords,vorname,nachname,tel){document.getElementById('contactModal').style.display='none';const finishModal=document.getElementById('finischModal');if(finishModal)finishModal.style.display="flex";window.adresse={vorname:vorname,nachname:nachname,telefon:tel};window.coords={latitude:coords.lat,longitude:coords.lng};if(typeof processCheckout0==="function"){try{const orderId=await processCheckout0();if(orderId){window.currentOrderId=orderId;document.getElementById('kontaktdaten').innerText="Gesendet!";listenForConfirmation(orderId);}else{throw new Error("Keine OrderId erhalten");}}catch(err){console.error(err);alert("Senden fehlgeschlagen.");orderGesendet=false;if(finishModal)finishModal.style.display="none";}}}
function listenForConfirmation(orderId){if(typeof io!=='undefined'){if(!socket)socket=io();socket.on('connect',()=>{console.log("Socket verbunden, trete Raum bei:",orderId);socket.emit('join_order_room',orderId);});if(socket.connected){socket.emit('join_order_room',orderId);}
socket.off('order_confirmed');socket.on('order_confirmed',(data)=>{console.log("Signal empfangen!",data);if(data.orderId==orderId){updateOrderUI(data.dauer);}});}else{console.error("Socket.io nicht geladen!");}}
function updateOrderUI(dauer){const kontaktdaten=document.getElementById('kontaktdaten');const waitText=document.getElementById('waitText');const loader=document.getElementById('modalLoader');const area=document.getElementById('confirmedArea');const display=document.getElementById('durationDisplay');if(waitText&&waitText.style.display!=="none"){if(kontaktdaten)kontaktdaten.innerText="Bestätigt! ✅";if(waitText)waitText.style.display="none";if(loader)loader.style.display="none";if(area)area.style.display="block";if(display)display.innerText=dauer;try{new Audio('https://actions.google.com/sounds/v1/alarms/beep_short.ogg').play();}catch(e){}}}
document.addEventListener("visibilitychange",async()=>{const orderId=currentOrderId||localStorage.getItem('lastOrderId');if(document.visibilityState==="visible"&&orderId){console.log("Handy aufgewacht – Prüfe Status für ID:",orderId);if(typeof io!=='undefined'){if(!socket||!socket.connected){listenForConfirmation(orderId);}}
try{const res=await fetch(`/api/orders/${orderId}`);if(res.ok){const order=await res.json();if(order.status==='angenommen'||order.status==='done'){updateOrderUI(order.dauerAbholung||order.dauer);}}}catch(err){console.error("Fehler beim Status-Check nach Aufwachen:",err);}}});function calculateDistance(lat1,lon1,lat2,lon2){const R=6371e3;const dLat=(lat2-lat1)*Math.PI/180;const dLon=(lon2-lon1)*Math.PI/180;const a=Math.sin(dLat/2)*Math.sin(dLat/2)+
Math.cos(lat1*Math.PI/180)*Math.cos(lat2*Math.PI/180)*Math.sin(dLon/2)*Math.sin(dLon/2);const c=2*Math.atan2(Math.sqrt(a),Math.sqrt(1-a));return R*c;}
function starteLiveTracking(orderId,initialCoords){if(!("geolocation"in navigator))return;if(initialCoords){sendGpsUpdate(orderId,initialCoords.lat,initialCoords.lng);}
navigator.geolocation.watchPosition(position=>{sendGpsUpdate(orderId,position.coords.latitude,position.coords.longitude);},null,{enableHighAccuracy:true});}
function sendGpsUpdate(orderId,lat,lng){const barLat=46.6225;const barLng=10.6941;const distanz=calculateDistance(lat,lng,barLat,barLng);fetch('/api/update-coords',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({orderId:orderId,coords:{lat:lat,lng:lng,distanz:Math.round(distanz)}})});}
function confirmSelection_(){if(currentEditIndex!==-1){orderCart.splice(currentEditIndex,1);currentEditIndex=-1;}
const existingIndex=orderCart.findIndex(item=>{const sameId=item.id===activePizza.id;const sameSize=item.orderDetails.size===activePizza.orderDetails.size;const sameExtras=JSON.stringify(item.orderDetails.extras?.sort())===JSON.stringify(activePizza.orderDetails.extras?.sort());const sameWithout=JSON.stringify(item.orderDetails.without?.sort())===JSON.stringify(activePizza.orderDetails.without?.sort());return sameId&&sameSize&&sameExtras&&sameWithout;});if(existingIndex!==-1){orderCart[existingIndex].orderDetails.quantity+=1;}else{activePizza.orderDetails.quantity=1;orderCart.push(activePizza);}
activePizza=null;renderCart();}